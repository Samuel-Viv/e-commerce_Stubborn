{% extends 'base.html.twig' %}

{% block title %}Page Produits{% endblock %}

{% block body %}

<div class="container">
    <h1>Page produits</h1>

    
        {% for product in products %}
        <div class="card">

            <img src="" alt="{{ product.name }}">
            <p>{{ product.name }}</p>
            <p>{{ product.price }}€</p>

            
            <button type="button" class="btn btn-primary"><a href="#" class="text-light text-decoration-none">Voir</a></button>

        </div>
        {% endfor%}
</div>
{% endblock %}
